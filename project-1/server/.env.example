# Copy to .env and set these values after registering an Azure AD app
CLIENT_ID=your-client-id
CLIENT_SECRET=your-client-secret
TENANT_ID=your-tenant-id
# The public URL for this server (used as redirectUri), e.g. http://localhost:3000
BASE_URL=http://localhost:3000
# Optional: port to run the server on
PORT=3000
